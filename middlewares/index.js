const test_middleware = require('./test_middleware')
const venues_middleware = require('./venues_middleware')
const login_middleware = require('./login_middleware')
const register_middleware = require('./register_middleware')

module.exports = {
  test_middleware,
  venues_middleware,
  login_middleware,
  register_middleware
}
