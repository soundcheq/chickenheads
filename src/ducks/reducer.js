const initialState = {}

//Action types

//Action Creators

//Object.assign cases

export default function reducer(state = initialState, action) {
  // console.log('this',action);
  switch (action.type) {
    default:
      return state
  }
}
