const initialState = {}

//Action types

// Reducer
export default function reducer(state = initialState, action) {
  // console.log('this',action);
  switch (action.type) {
    default:
      return state
  }
}

//Action Creators
